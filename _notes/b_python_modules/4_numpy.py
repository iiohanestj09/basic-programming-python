# ┌───────────────────────────────────┬────────────────────────────────────────────────────────────────┐
# │              Method               │ Deskripsi                                                      │
# ├───────────────────────────────────┼────────────────────────────────────────────────────────────────┤
# │ np.array()                        │ Membuat array NumPy dari list atau iterable lain               │
# │ np.zeros()                        │ Membuat array berisi nol                                       │
# │ np.ones()                         │ Membuat array berisi satu                                      │
# │ np.empty()                        │ Membuat array kosong (random value di memori)                  │
# │ np.full()                         │ Membuat array dengan nilai tetap                               │
# │ np.arange()                       │ Membuat array dengan rentang tertentu dan langkah spesifik     │
# │ np.linspace()                     │ Membuat array berisi nilai-nilai terpisah merata               │
# │ np.eye()                          │ Membuat matriks identitas                                      │
# │ np.reshape()                      │ Mengubah bentuk (shape) array tanpa mengubah isinya            │
# │ np.ravel()                        │ Mengubah array multi-dimensi jadi 1D (flatten)                 │
# │ np.transpose()                    │ Transposisi array (tukar baris dan kolom)                      │
# │ np.swapaxes()                     │ Menukar dua axis dalam array                                   │
# │ np.concatenate()                  │ Menggabungkan beberapa array                                   │
# │ np.stack()                        │ Menumpuk array sepanjang axis baru                             │
# │ np.split()                        │ Memisahkan array menjadi beberapa bagian                       │
# │ np.tile()                         │ Duplikasi array sesuai pola                                    │
# │ np.repeat()                       │ Ulangi elemen dalam array                                      │
# │ np.sort()                         │ Mengurutkan elemen array                                       │
# │ np.argsort()                      │ Indeks pengurutan elemen array                                 │
# │ np.argmin(), np.argmax()          │ Indeks dari nilai minimum/maksimum                             │
# │ np.mean(), np.std(), np.var()     │ Rata-rata, standar deviasi, dan variansi array                 │
# │ np.sum(), np.prod()               │ Penjumlahan dan hasil kali dari seluruh elemen                 │
# │ np.min(), np.max()                │ Nilai minimum dan maksimum dari array                          │
# │ np.dot(), np.matmul()             │ Perkalian matriks / dot product                                │
# │ np.where()                        │ Menemukan posisi berdasarkan kondisi                           │
# │ np.unique()                       │ Mengambil elemen unik dari array                               │
# │ np.isnan(), np.isinf()            │ Mengecek elemen NaN atau Inf                                   │
# └───────────────────────────────────┴────────────────────────────────────────────────────────────────┘
