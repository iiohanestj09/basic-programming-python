# MANIPULASI DAN ANALISIS DATA

# ┌──────────────────────────────────┬───────────────────────────────────────────────────────────────┐
# │        Fungsi/Method             │ Deskripsi                                                     │
# ├──────────────────────────────────┼───────────────────────────────────────────────────────────────┤
# │ pd.DataFrame(), pd.Series()      │ Membuat struktur data tabular dan 1D                          │
# │ df.head(), df.tail()             │ Menampilkan baris awal/akhir                                  │
# │ df.info(), df.describe()         │ Info struktur dan statistik ringkas                           │
# │ df.shape, df.columns             │ Dimensi dan nama kolom                                        │
# │ df.loc[], df.iloc[]              │ Akses data berdasarkan label atau indeks                      │
# │ df.at[], df.iat[]                │ Akses cepat satu elemen                                       │
# │ df.drop(), df.rename()           │ Hapus atau ubah nama kolom/baris                              │
# │ df.fillna(), df.dropna()         │ Isi atau hapus nilai kosong                                   │
# │ df.isnull(), df.notnull()        │ Deteksi nilai kosong                                          │
# │ df.sort_values(),                │ Urutkan berdasarkan nilai atau indeks                         │                   
# │   df.sort_index()                │                                                               │
# │ df.groupby()                     │ Kelompokkan data untuk agregasi                               │
# │ df.agg(), df.apply()             │ Terapkan fungsi custom atau agregasi                          │
# │ df.merge(), df.join(),           │ Gabungkan beberapa dataframe                                  │
# │   pd.concat()                    │                                                               │
# │ df.pivot(), df.melt()            │ Ubah bentuk data (pivoting/unpivoting)                        │
# │ df.duplicated(),                 │ Deteksi dan hapus duplikat                                    │ 
# │   df.drop_duplicates()           │                                                               │
# │ df.to_csv(), pd.read_csv()       │ Ekspor dan impor file CSV                                     │
# │ pd.read_excel(), df.to_excel()   │ Baca dan tulis file Excel                                     │
# │ pd.read_json(), df.to_json()     │ Baca dan tulis file JSON                                      │
# │ pd.date_range(),                 │ Manipulasi waktu dan tanggal                                  │  
# │   pd.to_datetime()               │                                                               │
# └──────────────────────────────────┴───────────────────────────────────────────────────────────────┘
