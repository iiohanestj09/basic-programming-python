# ┌───────────────────────────────┬───────────────────────────────────────────────────────────────────────┐
# │           Exception           │ Deskripsi                                                             │
# ├───────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
# │ ArithmeticError               │ Kesalahan dalam perhitungan numerik                                   │
# │ AssertionError                │ Gagalnya pernyataan assert                                            │
# │ AttributeError                │ Gagal akses atau penugasan atribut objek                              │
# │ Exception                     │ Kelas dasar untuk semua exception                                     │
# │ EOFError                      │ Input mencapai akhir file (EOF) tanpa data lagi                       │
# │ FloatingPointError            │ Kesalahan dalam perhitungan pecahan (floating point)                  │
# │ GeneratorExit                 │ Generator ditutup menggunakan close()                                 │
# │ ImportError                   │ Modul yang diimpor tidak ditemukan                                    │
# │ IndentationError              │ Penulisan indentasi (spasi/tab) tidak benar                           │
# │ IndexError                    │ Indeks yang diminta tidak ada dalam urutan (list/tuple)               │
# │ KeyError                      │ Kunci tidak ditemukan dalam dictionary                                │
# │ KeyboardInterrupt             │ Pengguna menekan Ctrl+C, Ctrl+Z, atau tombol interrupt                │
# │ LookupError                   │ Kelas dasar untuk exception lookup seperti IndexError dan KeyError    │
# │ MemoryError                   │ Program kehabisan memori                                              │
# │ NameError                     │ Variabel atau nama tidak dikenal                                      │
# │ NotImplementedError           │ Metode belum diimplementasikan di subclass                            │
# │ OSError                       │ Kesalahan terkait operasi sistem atau file                            │
# │ OverflowError                 │ Hasil perhitungan terlalu besar untuk tipe data yang digunakan        │
# │ ReferenceError                │ Referensi lemah ke objek sudah tidak ada                              │
# │ RuntimeError                  │ Kesalahan umum yang tidak termasuk kategori lain                      │
# │ StopIteration                 │ Iterator tidak memiliki elemen lagi saat dipanggil `next()`           │
# │ SyntaxError                   │ Kesalahan sintaks dalam kode Python                                   │
# │ TabError                      │ Inconsistensi penggunaan tab dan spasi pada indentasi                 │
# │ SystemError                   │ Terjadi kesalahan sistem internal Python                              │
# │ SystemExit                    │ Fungsi `sys.exit()` dipanggil untuk keluar dari program               │
# │ TypeError                     │ Operasi dilakukan pada tipe data yang tidak cocok                     │
# │ UnboundLocalError             │ Variabel lokal dipanggil sebelum diberikan nilai                      │
# │ UnicodeError                  │ Kesalahan terkait karakter Unicode                                    │
# │ UnicodeEncodeError            │ Gagal mengubah karakter ke Unicode encoded format                     │
# │ UnicodeDecodeError            │ Gagal mengubah encoded bytes ke karakter Unicode                      │
# │ UnicodeTranslateError         │ Gagal menerjemahkan karakter Unicode                                  │
# │ ValueError                    │ Nilai yang diberikan tidak sesuai dengan tipe yang diharapkan         │
# │ ZeroDivisionError             │ Pembagian dengan angka nol                                            │
# └───────────────────────────────┴───────────────────────────────────────────────────────────────────────┘
